name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install ShellCheck and ffmpeg
        run: sudo apt-get update && sudo apt-get install -y shellcheck ffmpeg

      - name: Run all test suites
        run: ./tests/run-all.sh
